apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: sql-ns

spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi


